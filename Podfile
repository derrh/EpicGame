# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'EpicGame' do
  use_frameworks!

  # Pods for EpicGame
  pod 'ReactiveCocoa'

  target 'EpicGameTests' do
    inherit! :search_paths
  end
end
